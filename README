This is an example of a RapidSMS project, as it might (currently) be
deployed in-country. When things settle down a bit, the contrib- repo
will be submoduled into lib/rapidsms/contrib, and the entire thing can
be bundled into a deb, rpm, .tar.gz, or whatever.

